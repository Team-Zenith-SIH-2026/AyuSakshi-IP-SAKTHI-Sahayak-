fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
pgvector>=0.3.0
rank-bm25>=0.2.2
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ML / retrieval stack.
# torch is installed separately in the Dockerfile from the CPU wheel index
# (the default PyPI wheel pulls ~2.5GB of CUDA libraries we cannot use).
# transformers is pinned below 5.x: the 5.x line requires a newer torch than the
# CPU wheel we install and fails at import with "name 'nn' is not defined".
sentence-transformers>=3.0.0,<4.0.0
transformers>=4.44.0,<5.0.0
numpy>=1.26.0,<3.0.0

# PostgreSQL driver. Without this get_db_connection() returns None and the
# entire pgvector corpus is silently bypassed in favour of an in-memory list.
psycopg2-binary>=2.9.9

# PDF text extraction for document ingestion.
PyMuPDF>=1.24.0
